@model IEnumerable<S_FOOD.Models.VIP>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<p>
    <button class="btn btn-warning">@Html.ActionLink("Thêm mới", "Create")</button>
</p>
<table class="table table-bordered table-hover table-striped" style="font-size:14px">
    <thead style="background-color:cornflowerblue">
        <tr>
            <th class="center">
                Level
            </th>
            <th class="center">
                Giá
            </th>
            <th class="center">Tùy chọn</th>
        </tr>
    </thead>

    @foreach (var item in Model)
    {
        <tr>
            <td class="center">
                @Html.DisplayFor(modelItem => item.LevelVip)
            </td>
            <td class="center">
                @Html.DisplayFor(modelItem => item.SoTienCanTra) VNĐ
            </td>
            <td class="center">
                <button class="btn btn-success">@Html.ActionLink("Sửa", "Edit", new { id = item.LevelVip }) </button>
                <button class="btn btn-primary">@Html.ActionLink("Chi tiết", "Details", new { id = item.LevelVip }) </button>
                <button class="btn btn-danger">@Html.ActionLink("Xóa", "Delete", new { id = item.LevelVip })</button>
            </td>
        </tr>
    }

</table>
<h5 class="center">@ViewBag.ErrorBody </h5>


    





